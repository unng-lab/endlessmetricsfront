# EndlessMetrics Frontend

Static admin/test frontend used by the short e2e suite.

```powershell
npm install
npm run dev
npm run test:e2e
```

Use `?api=http://host:8080` to point the frontend at a backend.
